# !/usr/bin/env python
#coding=utf8
import os
import csv
BUFFER_SIZE = 100

def transform():
    c_file = open('test')
    out_file = open('data','wr')
    reader = csv.reader(c_file)
    writer = csv.writer(out_file, delimiter=',')
    writer.writerow(['id','country','age','gender','education'])
    lineno = 0
    for row in reader:
        writer.writerow(cbuffer)
            cbuffer = []
    c_file.close()
    out_file.close()
    
if __name__ == '__main__':
    transform()
